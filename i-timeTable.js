// function itimeTable(multiplier) {
//     for (let start = 1; start <= 12; start++) {
//         console.log(start + ' x '+ multiplier + ' = ' + (start * multiplier));   
// }
// }

// While loop

function itimeTable(){
    console.log('====VARIABLE-LESS FUNCTION===')
    let start = 1
    while (start > 0 && start <= 12) {
        console.log(start + ' x 2 = ' + (start * 2))
        start++
    }
}